# ManlyCam Agent Configuration
# Copy this file to config.toml and fill in your values.
# Format: TOML (https://toml.io)
#
# Field names here are authoritative — the Go config struct in Story 3.1 will
# be implemented to match exactly these names. Do not rename fields.

[stream]
# Output frame width in pixels
width = 2328

# Output frame height in pixels
height = 1748

# Target frame rate (fps)
framerate = 30

# Video codec passed to rpicam-vid (e.g. "mjpeg", "h264")
codec = "mjpeg"

# Horizontally flip the camera image (true/false)
hflip = true

# Vertically flip the camera image (true/false)
vflip = true

# Local TCP port rpicam-vid listens on; the frp stream tunnel connects here.
# rpicam-vid command built by the agent:
#   rpicam-vid -t 0 --width {width} --height {height} --framerate {framerate} \
#     --codec {codec} [--hflip] [--vflip] --inline --listen \
#     -o tcp://0.0.0.0:{output_port}
output_port = 5000

[frp]
# Hostname or IP address of the ManlyCam server running frps
server_addr = "upstream.example.com"

# Port frps is listening on for client connections
server_port = 7000

# Authentication token — must match the auth_token set in frps config on the server
# Note: field name is auth_token (NOT token)
auth_token = "change-me-to-a-random-secret"

[frp.stream]
# Remote port frps exposes for stream ingestion; ffmpeg on the server connects here
remote_port = 11935

[frp.api]
# Local port the Pi agent's HTTP server listens on (receives camera control commands)
local_port = 8080

# Remote port frps exposes for the API proxy; the Hono server connects here
remote_port = 11936

[update]
# GitHub Releases API URL for update checks
# The agent fetches this URL to discover the latest release asset download URL
update_url = "https://api.github.com/repos/zikeji/ManlyCam/releases/latest"
