# ManlyCam Monorepo — Workspace-level environment variables
# Copy this file to .env and fill in values for local development.
# App-specific env vars live in apps/server/.env.example and apps/web/.env.example

# Not used at workspace root — see individual app .env.example files
