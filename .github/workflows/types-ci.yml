name: Types CI

on:
  push:
    branches: [main]
    paths:
      - 'packages/types/**'
      - '.github/workflows/types-ci.yml'
  pull_request:
    paths:
      - 'packages/types/**'
      - '.github/workflows/types-ci.yml'

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: latest
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @manlycam/types typecheck
